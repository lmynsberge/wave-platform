# wave-core (Rust)

Domain core service. SPEC-001 scope: health + ping endpoints.

## Commands
- Run: `cargo run` (env: `CORE_PORT`, default 8081)
- Test: `cargo test`
- Lint: `cargo clippy -- -D warnings` and `cargo fmt --check`
