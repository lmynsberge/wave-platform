# wave-web (React)

Frontend. SPEC-001 scope: single page rendering /api/ping with ok + degraded states.

## Commands
- Run: `npm run dev` (proxies /api to localhost:8080)
- Test: `npm test`
- Lint/typecheck: `npm run lint`
- Build: `npm run build`
