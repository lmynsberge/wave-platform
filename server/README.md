# wave-server (TypeScript / fastify)

BFF. SPEC-001 scope: /health and /api/ping proxy to core.

## Commands
- Run: `npm run dev` (env: `CORE_URL` default http://localhost:8081, `PORT` default 8080)
- Test: `npm test`
- Lint/typecheck: `npm run lint`
